<?xml version="1.0" encoding="UTF-8"?>
<!--
  ~ Copyright 2025 IKE Community
  ~
  ~ Licensed under the Apache License, Version 2.0 (the "License");
  ~ you may not use this file except in compliance with the License.
  ~ You may obtain a copy of the License at
  ~
  ~     http://www.apache.org/licenses/LICENSE-2.0
  ~
  ~ Unless required by applicable law or agreed to in writing, software
  ~ distributed under the License is distributed on an "AS IS" BASIS,
  ~ WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
  ~ See the License for the specific language governing permissions and
  ~ limitations under the License.
  -->
<project xmlns="http://maven.apache.org/POM/4.1.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.1.0
         http://maven.apache.org/xsd/maven-4.1.0.xsd">
    <modelVersion>4.1.0</modelVersion>

    <groupId>network.ike</groupId>
    <artifactId>ike-parent</artifactId>
    <version>1.1.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>IKE Documentation Parent</name>
    <description>Parent POM for IKE Community projects with AsciiDoc documentation pipeline</description>
    <url>https://github.com/IKE-Network</url>

    <licenses>
        <license>
            <name>Apache License, Version 2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <organization>
        <name>IKE Community</name>
        <url>https://ike.network</url>
    </organization>

    <distributionManagement>
        <repository>
            <id>ike-releases</id>
            <url>${deploy.release.url}</url>
        </repository>
        <snapshotRepository>
            <id>ike-snapshots</id>
            <url>${deploy.snapshot.url}</url>
        </snapshotRepository>
    </distributionManagement>

    <properties>
        <!-- Build Configuration -->
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

        <!-- IKE Build Infrastructure Versions -->
        <ike-build-tools.version>1.0.0-SNAPSHOT</ike-build-tools.version>

        <!-- Maven Plugin Versions -->
        <maven-dependency-plugin.version>3.8.1</maven-dependency-plugin.version>
        <maven-resources-plugin.version>3.3.1</maven-resources-plugin.version>
        <maven-clean-plugin.version>3.4.0</maven-clean-plugin.version>

        <!-- AsciiDoc Toolchain Versions -->
        <asciidoctor-maven-plugin.version>3.1.0</asciidoctor-maven-plugin.version>
        <asciidoctorj.version>2.5.13</asciidoctorj.version>
        <asciidoctorj-pdf.version>2.3.23</asciidoctorj-pdf.version>
        <asciidoctorj-diagram.version>2.3.1</asciidoctorj-diagram.version>
        <jruby.version>10.0.3.0</jruby.version>

        <!-- Diagram Tool Configuration — Kroki server -->
        <!-- Override with -Dkroki.server.url=http://localhost:8000 for local testing -->
        <kroki.server.url>https://kroki.komet.sh</kroki.server.url>
        <asciidoctor.diagram.plantuml.format>svg</asciidoctor.diagram.plantuml.format>

        <!-- IKE Documentation Resources — unpacked from ike-doc-resources JAR -->
        <ike.doc.resources.directory>${project.build.directory}/ike-doc-resources</ike.doc.resources.directory>

        <!-- IKE AsciiDoc Extensions -->
        <ike.extensions.directory>${ike.doc.resources.directory}/extensions</ike.extensions.directory>

        <!-- IKE Shared AsciiDoc Resources (docinfo.html, etc.)             -->
        <!-- All projects get shared docinfo (Koncept CSS, etc.) via the    -->
        <!-- docinfodir attribute.  Projects needing ADDITIONAL docinfo     -->
        <!-- can override docinfodir and use antrun to merge shared +       -->
        <!-- project-specific files into a staging directory.               -->
        <ike.shared.asciidoc.directory>${ike.doc.resources.directory}/shared-asciidoc</ike.shared.asciidoc.directory>

        <!-- Documentation Paths (Standard Maven) -->
        <asciidoc.source.directory>${project.basedir}/src/docs/asciidoc</asciidoc.source.directory>
        <asciidoc.output.directory>${project.build.directory}/generated-docs</asciidoc.output.directory>
        <asciidoc.generated.directory>${project.build.directory}/generated-sources/asciidoc</asciidoc.generated.directory>
        <asciidoc.theme.directory>${ike.doc.resources.directory}/theme</asciidoc.theme.directory>

        <!-- Font Configuration -->
        <fonts.output.directory>${project.build.directory}/fonts</fonts.output.directory>

        <!-- Maven Enforcer Plugin Version -->
        <maven-enforcer-plugin.version>3.5.0</maven-enforcer-plugin.version>
        <exec-maven-plugin.version>3.5.0</exec-maven-plugin.version>
        <build-helper-maven-plugin.version>3.6.0</build-helper-maven-plugin.version>
        <maven-assembly-plugin.version>3.7.1</maven-assembly-plugin.version>

        <!-- ── External PDF Renderer Configuration ──────────────────── -->
        <!-- Override with -Dprince.executable=/usr/local/bin/prince etc. -->
        <prince.executable>prince</prince.executable>
        <antennahouse.executable>AHFCmd</antennahouse.executable>
        <weasyprint.executable>weasyprint</weasyprint.executable>
        <xep.executable>xep</xep.executable>
        <xep.home>/Library/Java/xep</xep.home>

        <!-- ── DocBook → XSL-FO → PDF Toolchain ──────────────────────── -->
        <!-- Saxon-HE:      Resolved as xml-maven-plugin plugin dependency        -->
        <!--                 (Maven Central → Nexus proxy). No local JAR needed.  -->
        <!-- DocBook XSL:   Unpacked from ike-docbook-xsl artifact.               -->
        <!--                 Includes stock 1.79.2 stylesheets + IKE FO layer.    -->
        <!--                 Build once, deploy to Nexus, consume everywhere.      -->
        <saxon-he.version>12.5</saxon-he.version>
        <fop.version>2.11</fop.version>
        <xml-maven-plugin.version>1.1.0</xml-maven-plugin.version>
        <docbook-xsl.ike.version>1.79.2-ike.2-SNAPSHOT</docbook-xsl.ike.version>
        <docbook.xsl.directory>${project.build.directory}/docbook-xsl</docbook.xsl.directory>

        <!-- Intermediate directories for the DocBook pipeline -->
        <docbook.output.directory>${asciidoc.output.directory}/docbook</docbook.output.directory>
        <fo.output.directory>${asciidoc.output.directory}/fo</fo.output.directory>

        <!-- Name of the master document (without extension).             -->
        <!-- Most projects have index.adoc; override for other names.     -->
        <pdf.source.document>index</pdf.source.document>

        <!-- Output PDF filename (without extension).                    -->
        <!-- Defaults to the Maven artifactId (always filename-safe).    -->
        <!-- Override per-project in <properties> or via                  -->
        <!-- -Dike.document.name=My-Document on the command line.        -->
        <ike.document.name>${project.artifactId}</ike.document.name>

        <!-- Intermediate directory for print-ready HTML + assets         -->
        <pdf.html.directory>${asciidoc.output.directory}/pdf-html</pdf.html.directory>

        <!-- CSS Paged Media stylesheet for external renderers            -->
        <!-- Uses the parent module path so child projects resolve it    -->
        <!-- correctly (same pattern as ike.extensions.directory).       -->
        <pdf.print.stylesheet.directory>${ike.doc.resources.directory}/theme</pdf.print.stylesheet.directory>
        <pdf.print.stylesheet>${pdf.print.stylesheet.directory}/ike-print.css</pdf.print.stylesheet>

        <!-- Assembly descriptor directory for ZIP artifact creation -->
        <ike.assembly.directory>${ike.doc.resources.directory}/assembly</ike.assembly.directory>

        <!-- ── Renderer Skip Flags ─────────────────────────────────────── -->
        <!-- All renderers off by default. Activate via profile or -D.      -->
        <ike.skip.html>true</ike.skip.html>
        <ike.skip.html-single>true</ike.skip.html-single>
        <ike.skip.prawn>true</ike.skip.prawn>
        <ike.skip.prince>true</ike.skip.prince>
        <ike.skip.ah>true</ike.skip.ah>
        <ike.skip.weasyprint>true</ike.skip.weasyprint>
        <ike.skip.xep>true</ike.skip.xep>
        <ike.skip.fop>true</ike.skip.fop>
        <ike.skip.pdf-default>true</ike.skip.pdf-default>
        <ike.skip.docbook>true</ike.skip.docbook>
        <ike.skip.asciidoc-zip>true</ike.skip.asciidoc-zip>

        <!-- Default renderer for the pdf-default copy. POM-level properties
             do NOT trigger property-based profile activation, so this is safe.
             Overridden by -Dike.pdf.default=RENDERER on the command line. -->
        <ike.pdf.default>prawn</ike.pdf.default>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- AsciiDoc Documentation Dependencies -->
            <dependency>
                <groupId>org.asciidoctor</groupId>
                <artifactId>asciidoctorj</artifactId>
                <version>${asciidoctorj.version}</version>
            </dependency>
            <dependency>
                <groupId>org.asciidoctor</groupId>
                <artifactId>asciidoctorj-pdf</artifactId>
                <version>${asciidoctorj-pdf.version}</version>
            </dependency>
            <dependency>
                <groupId>org.asciidoctor</groupId>
                <artifactId>asciidoctorj-diagram</artifactId>
                <version>${asciidoctorj-diagram.version}</version>
            </dependency>

            <!-- NOTE: ike-bom is NOT imported here — Maven 4 warns against     -->
            <!-- BOM imports from within the reactor. Instead, the versions for  -->
            <!-- ike-build-standards and ike-doc-resources are managed inline.   -->

            <!-- Noto Fonts for PDF generation (minimal subset) -->
            <dependency>
                <groupId>network.ike</groupId>
                <artifactId>minimal-fonts</artifactId>
                <version>1.0.0-SNAPSHOT</version>
            </dependency>

            <!-- DocBook XSL 1.79.2 + IKE FO customization layer -->
            <dependency>
                <groupId>network.ike</groupId>
                <artifactId>docbook-xsl</artifactId>
                <version>${docbook-xsl.ike.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!-- Fonts for PDF generation (required for Prawn PDF profile) -->
        <dependency>
            <groupId>network.ike</groupId>
            <artifactId>minimal-fonts</artifactId>
        </dependency>
    </dependencies>

    <build>
        <pluginManagement>
            <plugins>
                <!-- Maven Dependency Plugin - Unpack Documentation Dependencies -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-dependency-plugin</artifactId>
                    <version>${maven-dependency-plugin.version}</version>
                    <executions>
                        <execution>
                            <id>unpack-doc-resources</id>
                            <phase>initialize</phase>
                            <goals><goal>unpack</goal></goals>
                            <configuration>
                                <artifactItems>
                                    <artifactItem>
                                        <groupId>network.ike</groupId>
                                        <artifactId>ike-doc-resources</artifactId>
                                        <version>1.0.0-SNAPSHOT</version>
                                        <type>jar</type>
                                        <overWrite>true</overWrite>
                                        <outputDirectory>${ike.doc.resources.directory}</outputDirectory>
                                        <excludes>META-INF/**</excludes>
                                    </artifactItem>
                                </artifactItems>
                            </configuration>
                        </execution>
                        <execution>
                            <id>unpack-doc-dependencies</id>
                            <phase>generate-sources</phase>
                            <goals>
                                <goal>unpack-dependencies</goal>
                            </goals>
                            <configuration>
                                <classifier>asciidoc</classifier>
                                <type>zip</type>
                                <outputDirectory>${asciidoc.generated.directory}</outputDirectory>
                                <useSubDirectoryPerArtifact>true</useSubDirectoryPerArtifact>
                                <stripVersion>true</stripVersion>
                                <stripType>true</stripType>
                                <includeGroupIds>network.ike</includeGroupIds>
                            </configuration>
                        </execution>
                        <execution>
                            <id>unpack-build-tools</id>
                            <phase>initialize</phase>
                            <goals>
                                <goal>unpack</goal>
                            </goals>
                            <configuration>
                                <artifactItems>
                                    <artifactItem>
                                        <groupId>network.ike</groupId>
                                        <artifactId>ike-build-tools</artifactId>
                                        <version>${ike-build-tools.version}</version>
                                        <classifier>tools</classifier>
                                        <type>zip</type>
                                        <overWrite>true</overWrite>
                                        <outputDirectory>${project.build.directory}/build-tools</outputDirectory>
                                    </artifactItem>
                                </artifactItems>
                            </configuration>
                        </execution>
                        <execution>
                            <id>unpack-claude-standards</id>
                            <phase>validate</phase>
                            <goals>
                                <goal>unpack-dependencies</goal>
                            </goals>
                            <configuration>
                                <includeArtifactIds>ike-build-standards</includeArtifactIds>
                                <includeClassifiers>claude</includeClassifiers>
                                <includeTypes>zip</includeTypes>
                                <outputDirectory>${project.basedir}/.claude/standards</outputDirectory>
                                <overWriteReleases>true</overWriteReleases>
                                <overWriteSnapshots>true</overWriteSnapshots>
                            </configuration>
                        </execution>
                        <execution>
                            <id>unpack-fonts</id>
                            <phase>generate-resources</phase>
                            <goals>
                                <goal>unpack</goal>
                            </goals>
                            <configuration>
                                <artifactItems>
                                    <artifactItem>
                                        <groupId>network.ike</groupId>
                                        <artifactId>minimal-fonts</artifactId>
                                        <version>1.0.0-SNAPSHOT</version>
                                        <type>jar</type>
                                        <outputDirectory>${fonts.output.directory}</outputDirectory>
                                        <includes>**/*.ttf</includes>
                                        <excludes>META-INF/**</excludes>
                                    </artifactItem>
                                </artifactItems>
                                <overWriteReleases>true</overWriteReleases>
                                <overWriteSnapshots>true</overWriteSnapshots>
                            </configuration>
                        </execution>
                    </executions>
                </plugin>

                <!-- Maven Resources Plugin -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-resources-plugin</artifactId>
                    <version>${maven-resources-plugin.version}</version>
                </plugin>

                <!-- Maven Clean Plugin -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-clean-plugin</artifactId>
                    <version>${maven-clean-plugin.version}</version>
                </plugin>
                <!-- Lock default lifecycle plugin versions (suppresses Maven 4 warning) -->
                <plugin><artifactId>maven-compiler-plugin</artifactId><version>3.14.0</version></plugin>
                <plugin><artifactId>maven-surefire-plugin</artifactId><version>3.5.2</version></plugin>
                <plugin><artifactId>maven-jar-plugin</artifactId><version>3.4.2</version></plugin>
                <plugin><artifactId>maven-install-plugin</artifactId><version>3.1.3</version></plugin>
                <plugin><artifactId>maven-deploy-plugin</artifactId><version>3.1.3</version></plugin>

                <!-- Exec Maven Plugin — invokes external PDF renderers -->
                <plugin>
                    <groupId>org.codehaus.mojo</groupId>
                    <artifactId>exec-maven-plugin</artifactId>
                    <version>${exec-maven-plugin.version}</version>
                </plugin>

                <!-- Build Helper Plugin — attach classified PDF artifacts -->
                <plugin>
                    <groupId>org.codehaus.mojo</groupId>
                    <artifactId>build-helper-maven-plugin</artifactId>
                    <version>${build-helper-maven-plugin.version}</version>
                </plugin>

                <!-- Assembly Plugin — create classified ZIP artifacts -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-assembly-plugin</artifactId>
                    <version>${maven-assembly-plugin.version}</version>
                </plugin>

                <!-- XML Maven Plugin — XSLT transforms (DocBook → XSL-FO) -->
                <!-- Saxon-HE is resolved as a plugin dependency via Maven  -->
                <!-- Central / Nexus — no local JAR download required.      -->
                <plugin>
                    <groupId>org.codehaus.mojo</groupId>
                    <artifactId>xml-maven-plugin</artifactId>
                    <version>${xml-maven-plugin.version}</version>
                    <dependencies>
                        <dependency>
                            <groupId>net.sf.saxon</groupId>
                            <artifactId>Saxon-HE</artifactId>
                            <version>${saxon-he.version}</version>
                        </dependency>
                    </dependencies>
                </plugin>

                <!-- Maven Enforcer Plugin - Validate prerequisites -->
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-enforcer-plugin</artifactId>
                    <version>${maven-enforcer-plugin.version}</version>
                    <executions>
                        <execution>
                            <id>enforce-prerequisites</id>
                            <goals>
                                <goal>enforce</goal>
                            </goals>
                            <configuration>
                                <rules>
                                    <requireMavenVersion>
                                        <version>[4.0.0-rc-1,)</version>
                                        <message>
IKE projects require Maven 4+.
Download: https://maven.apache.org/download.cgi
                                        </message>
                                    </requireMavenVersion>
                                    <requireJavaVersion>
                                        <version>[21,)</version>
                                        <message>
IKE projects require Java 21+ (JRuby 10 baseline).
  macOS:  brew install openjdk
  Ubuntu: sudo apt install openjdk-21-jdk
                                        </message>
                                    </requireJavaVersion>
                                </rules>
                            </configuration>
                        </execution>
                    </executions>
                </plugin>

                <!-- AsciiDoctor Maven Plugin -->
                <plugin>
                    <groupId>org.asciidoctor</groupId>
                    <artifactId>asciidoctor-maven-plugin</artifactId>
                    <version>${asciidoctor-maven-plugin.version}</version>
                    <dependencies>
                        <!-- JRuby 10.x — replaces sun.misc.Unsafe with
                             java.lang.foreign / VarHandle APIs (JDK 21+).
                             Must appear before asciidoctorj to override its
                             transitive JRuby 9.x dependency. -->
                        <dependency>
                            <groupId>org.jruby</groupId>
                            <artifactId>jruby</artifactId>
                            <version>${jruby.version}</version>
                        </dependency>
                        <dependency>
                            <groupId>org.asciidoctor</groupId>
                            <artifactId>asciidoctorj</artifactId>
                            <version>${asciidoctorj.version}</version>
                            <exclusions>
                                <exclusion>
                                    <groupId>org.jruby</groupId>
                                    <artifactId>jruby-base</artifactId>
                                </exclusion>
                            </exclusions>
                        </dependency>
                        <dependency>
                            <groupId>org.asciidoctor</groupId>
                            <artifactId>asciidoctorj-pdf</artifactId>
                            <version>${asciidoctorj-pdf.version}</version>
                        </dependency>
                        <dependency>
                            <groupId>org.asciidoctor</groupId>
                            <artifactId>asciidoctorj-diagram</artifactId>
                            <version>${asciidoctorj-diagram.version}</version>
                        </dependency>
                        <!-- Koncept Java extension — auto-registered via SPI -->
                        <dependency>
                            <groupId>network.ike</groupId>
                            <artifactId>koncept-asciidoc-extension</artifactId>
                            <version>1.1.0-SNAPSHOT</version>
                        </dependency>
                    </dependencies>
                    <configuration>
                        <sourceDirectory>${asciidoc.source.directory}</sourceDirectory>
                        <baseDir>${asciidoc.source.directory}</baseDir>
                        <requires>
                            <require>asciidoctor-diagram</require>
                        </requires>
                        <attributes>
                            <!-- Standard Maven attributes -->
                            <project-version>${project.version}</project-version>
                            <project-name>${project.name}</project-name>

                            <!-- Path attributes for includes -->
                            <generated>${asciidoc.generated.directory}</generated>
                            <resources>${project.basedir}/src/docs/resources</resources>

                            <!-- Shared docinfo location -->
                            <docinfodir>${ike.shared.asciidoc.directory}</docinfodir>

                            <!-- Document attributes -->
                            <source-highlighter>coderay</source-highlighter>
                            <coderay-linenums-mode>table</coderay-linenums-mode>
                            <icons>font</icons>
                            <sectanchors>true</sectanchors>
                            <idprefix />
                            <idseparator>-</idseparator>
                            <docinfo>shared</docinfo>

                            <!-- Diagram configuration — Kroki server-side rendering -->
                            <!-- Delegates PlantUML and GraphViz diagrams to the     -->
                            <!-- Kroki server, eliminating local CLI tool requirements. -->
                            <diagram-server-url>${kroki.server.url}</diagram-server-url>
                            <diagram-server-type>kroki_io</diagram-server-type>
                            <diagram-format>svg</diagram-format>
                            <diagram-svg-type>inline</diagram-svg-type>

                            <!-- allow-uri-read required for Kroki HTTP requests -->
                            <allow-uri-read>true</allow-uri-read>
                        </attributes>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>

        <!-- ════════════════════════════════════════════════════════════════ -->
        <!-- Active plugins for all projects inheriting this parent.         -->
        <!--                                                                 -->
        <!-- Every renderer execution is declared here with a per-renderer   -->
        <!-- <skip> flag that defaults to true. Profiles are thin toggles    -->
        <!-- that flip the relevant skip flag to false.                      -->
        <!-- ════════════════════════════════════════════════════════════════ -->
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-enforcer-plugin</artifactId>
            </plugin>

            <!-- ── maven-dependency-plugin ───────────────────────────────── -->
            <!-- Unpack fonts (always) and DocBook XSL (when XEP or FOP)     -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <executions>
                    <!-- Unpack shared documentation resources (themes, assembly descriptors,
                         renderer configs, etc.) from ike-doc-resources JAR.
                         Runs at initialize phase so resources are available for all
                         subsequent build phases. -->
                    <execution>
                        <id>unpack-doc-resources</id>
                        <phase>initialize</phase>
                        <goals><goal>unpack</goal></goals>
                        <configuration>
                            <artifactItems>
                                <artifactItem>
                                    <groupId>network.ike</groupId>
                                    <artifactId>ike-doc-resources</artifactId>
                                    <version>1.0.0-SNAPSHOT</version>
                                    <type>jar</type>
                                    <overWrite>true</overWrite>
                                    <outputDirectory>${ike.doc.resources.directory}</outputDirectory>
                                    <excludes>META-INF/**</excludes>
                                </artifactItem>
                            </artifactItems>
                        </configuration>
                    </execution>
                    <!-- Unpack standards directly (unpack goal, not unpack-dependencies).
                         The pluginManagement unpack-dependencies execution only works for
                         child modules that declare ike-build-standards as a <dependency>.
                         This execution ensures ike-parent and java-parent themselves get
                         standards unpacked too. -->
                    <execution>
                        <id>unpack-claude-standards-direct</id>
                        <phase>validate</phase>
                        <goals><goal>unpack</goal></goals>
                        <configuration>
                            <artifactItems>
                                <artifactItem>
                                    <groupId>network.ike</groupId>
                                    <artifactId>ike-build-standards</artifactId>
                                    <version>4-SNAPSHOT</version>
                                    <classifier>claude</classifier>
                                    <type>zip</type>
                                    <overWrite>true</overWrite>
                                    <outputDirectory>${project.basedir}/.claude/standards</outputDirectory>
                                </artifactItem>
                            </artifactItems>
                        </configuration>
                    </execution>
                    <execution>
                        <id>unpack-build-tools</id>
                        <!-- inherits config from pluginManagement -->
                    </execution>
                    <execution>
                        <id>unpack-fonts</id>
                        <!-- inherits config from pluginManagement -->
                    </execution>
                    <execution>
                        <id>unpack-docbook-xsl</id>
                        <phase>generate-resources</phase>
                        <goals>
                            <goal>unpack</goal>
                        </goals>
                        <configuration>
                            <skip>${ike.skip.docbook}</skip>
                            <artifactItems>
                                <artifactItem>
                                    <groupId>network.ike</groupId>
                                    <artifactId>docbook-xsl</artifactId>
                                    <version>${docbook-xsl.ike.version}</version>
                                    <type>jar</type>
                                    <outputDirectory>${docbook.xsl.directory}</outputDirectory>
                                    <excludes>META-INF/**</excludes>
                                </artifactItem>
                            </artifactItems>
                            <overWriteReleases>true</overWriteReleases>
                            <overWriteSnapshots>true</overWriteSnapshots>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- ── maven-resources-plugin ───────────────────────────────── -->
            <!-- Asset staging for each renderer: config files, CSS, fonts.  -->
            <!-- Replaces maven-antrun-plugin — pure Maven, no Ant scripts.  -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-resources-plugin</artifactId>
                <executions>
                    <!-- XEP: Stage XEP font config + fonts.                      -->
                    <!--       XEP reads xep-ike-fonts.xml which uses             -->
                    <!--       xml:base="fonts/" to locate TTFs relative to it.   -->
                    <execution>
                        <id>prepare-xep-assets</id>
                        <phase>generate-resources</phase>
                        <goals><goal>copy-resources</goal></goals>
                        <configuration>
                            <skip>${ike.skip.xep}</skip>
                            <outputDirectory>${project.build.directory}/xep-config</outputDirectory>
                            <resources>
                                <resource>
                                    <directory>${ike.doc.resources.directory}/xep</directory>
                                    <includes><include>*.xml</include></includes>
                                </resource>
                                <resource>
                                    <directory>${xep.home}</directory>
                                    <includes><include>license.xml</include></includes>
                                </resource>
                                <resource>
                                    <directory>${fonts.output.directory}</directory>
                                    <includes><include>**/*.ttf</include></includes>
                                    <targetPath>fonts</targetPath>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>

                    <!-- FOP: Stage FOP config + fonts.                            -->
                    <!--       FOP reads fop-ike.xconf which uses                  -->
                    <!--       <font-base>fonts/</font-base> for TTF resolution.   -->
                    <execution>
                        <id>prepare-fop-assets</id>
                        <phase>generate-resources</phase>
                        <goals><goal>copy-resources</goal></goals>
                        <configuration>
                            <skip>${ike.skip.fop}</skip>
                            <outputDirectory>${project.build.directory}/fop-config</outputDirectory>
                            <resources>
                                <resource>
                                    <directory>${ike.doc.resources.directory}/fop</directory>
                                    <includes><include>*.xconf</include></includes>
                                </resource>
                                <resource>
                                    <directory>${fonts.output.directory}</directory>
                                    <includes><include>**/*.ttf</include></includes>
                                    <targetPath>fonts</targetPath>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>

                    <!-- Prince: Copy print CSS + fonts to pdf-html-prince dir -->
                    <execution>
                        <id>prepare-print-assets-prince</id>
                        <phase>prepare-package</phase>
                        <goals><goal>copy-resources</goal></goals>
                        <configuration>
                            <skip>${ike.skip.prince}</skip>
                            <outputDirectory>${asciidoc.output.directory}/pdf-html-prince</outputDirectory>
                            <resources>
                                <resource>
                                    <directory>${pdf.print.stylesheet.directory}</directory>
                                    <includes><include>ike-print.css</include></includes>
                                </resource>
                                <resource>
                                    <directory>${fonts.output.directory}</directory>
                                    <includes><include>**/*.ttf</include></includes>
                                    <targetPath>fonts</targetPath>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>

                    <!-- AH: Copy print CSS + fonts to pdf-html-ah dir -->
                    <execution>
                        <id>prepare-print-assets-ah</id>
                        <phase>prepare-package</phase>
                        <goals><goal>copy-resources</goal></goals>
                        <configuration>
                            <skip>${ike.skip.ah}</skip>
                            <outputDirectory>${asciidoc.output.directory}/pdf-html-ah</outputDirectory>
                            <resources>
                                <resource>
                                    <directory>${pdf.print.stylesheet.directory}</directory>
                                    <includes><include>ike-print.css</include></includes>
                                </resource>
                                <resource>
                                    <directory>${fonts.output.directory}</directory>
                                    <includes><include>**/*.ttf</include></includes>
                                    <targetPath>fonts</targetPath>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>

                    <!-- WeasyPrint: Copy print CSS + fonts to pdf-html-weasyprint dir -->
                    <execution>
                        <id>prepare-print-assets-weasyprint</id>
                        <phase>prepare-package</phase>
                        <goals><goal>copy-resources</goal></goals>
                        <configuration>
                            <skip>${ike.skip.weasyprint}</skip>
                            <outputDirectory>${asciidoc.output.directory}/pdf-html-weasyprint</outputDirectory>
                            <resources>
                                <resource>
                                    <directory>${pdf.print.stylesheet.directory}</directory>
                                    <includes><include>ike-print.css</include></includes>
                                </resource>
                                <resource>
                                    <directory>${fonts.output.directory}</directory>
                                    <includes><include>**/*.ttf</include></includes>
                                    <targetPath>fonts</targetPath>
                                </resource>
                            </resources>
                        </configuration>
                    </execution>

                </executions>
            </plugin>

            <!-- ── asciidoctor-maven-plugin ─────────────────────────────── -->
            <!-- All AsciiDoc conversions: HTML, Prawn PDF, print HTML for   -->
            <!-- external renderers, and DocBook for XSL-FO pipelines.       -->
            <plugin>
                <groupId>org.asciidoctor</groupId>
                <artifactId>asciidoctor-maven-plugin</artifactId>
                <executions>

                    <!-- ── HTML5 ───────────────────────────────────────── -->
                    <execution>
                        <id>generate-html-docs</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>process-asciidoc</goal>
                        </goals>
                        <configuration>
                            <skip>${ike.skip.html}</skip>
                            <backend>html5</backend>
                            <outputDirectory>${asciidoc.output.directory}/html</outputDirectory>
                            <!-- Register glossary Postprocessor for HTML only.
                                 The InlineMacro is auto-discovered via SPI.
                                 The Postprocessor cannot be registered via SPI
                                 because it crashes the asciidoctorj-pdf backend. -->
                            <extensions>
                                <extension>
                                    <className>network.ike.docs.koncept.KonceptGlossaryProcessor</className>
                                </extension>
                            </extensions>
                            <attributes>
                                <toc>left</toc>
                                <toclevels>3</toclevels>
                                <linkcss>false</linkcss>
                                <ike-pdf-renderer>html5</ike-pdf-renderer>
                            </attributes>
                        </configuration>
                    </execution>

                    <!-- ════════════════════════════════════════════════════ -->
                    <!-- Self-contained HTML (single file)                    -->
                    <!--                                                     -->
                    <!-- Same as HTML5 but with data-uri: all images are      -->
                    <!-- base64-encoded inline. Produces one self-contained   -->
                    <!-- HTML file suitable for sharing, email, or offline    -->
                    <!-- reading. Renamed to ${ike.document.name}.html by a  -->
                    <!-- subsequent exec step.                                -->
                    <!-- ════════════════════════════════════════════════════ -->
                    <execution>
                        <id>generate-html-single</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>process-asciidoc</goal>
                        </goals>
                        <configuration>
                            <skip>${ike.skip.html-single}</skip>
                            <backend>html5</backend>
                            <sourceDocumentName>${pdf.source.document}.adoc</sourceDocumentName>
                            <outputDirectory>${asciidoc.output.directory}/html-single</outputDirectory>
                            <extensions>
                                <extension>
                                    <className>network.ike.docs.koncept.KonceptGlossaryProcessor</className>
                                </extension>
                            </extensions>
                            <attributes>
                                <toc>left</toc>
                                <toclevels>3</toclevels>
                                <linkcss>false</linkcss>
                                <data-uri/>
                                <ike-pdf-renderer>html5</ike-pdf-renderer>
                            </attributes>
                        </configuration>
                    </execution>

                    <!-- ════════════════════════════════════════════════════ -->
                    <!-- PDF via asciidoctorj-pdf / Prawn (built-in, free)   -->
                    <!--                                                     -->
                    <!-- Uses the JRuby/Prawn backend directly. PlantUML     -->
                    <!-- and GraphViz diagrams render as SVG via Kroki.      -->
                    <!-- ════════════════════════════════════════════════════ -->
                    <execution>
                        <id>generate-pdf-docs</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>process-asciidoc</goal>
                        </goals>
                        <configuration>
                            <skip>${ike.skip.prawn}</skip>
                            <backend>pdf</backend>
                            <sourceDocumentName>${pdf.source.document}.adoc</sourceDocumentName>
                            <outputDirectory>${asciidoc.output.directory}/pdf-prawn</outputDirectory>
                            <attributes>
                                <pdf-themesdir>${asciidoc.theme.directory}</pdf-themesdir>
                                <pdf-theme>ike-default</pdf-theme>
                                <pdf-fontsdir>${fonts.output.directory}</pdf-fontsdir>
                                <allow-uri-read>true</allow-uri-read>
                                <media>prepress</media>
                                <optimize>true</optimize>
                                <ike-pdf-renderer>prawn</ike-pdf-renderer>
                            </attributes>
                        </configuration>
                    </execution>

                    <!-- ════════════════════════════════════════════════════ -->
                    <!-- PDF via Prince XML ($495 Desktop / $3800 Server)    -->
                    <!--                                                     -->
                    <!-- Pipeline: AsciiDoc → HTML5 → Prince → PDF          -->
                    <!-- Prince has its own CSS engine — no prawn-svg, no    -->
                    <!-- svgo. Full CSS Paged Media, PDF/UA, SVG with CSS.   -->
                    <!--                                                     -->
                    <!-- License tiers (princexml.com/purchase):             -->
                    <!--   Free non-commercial: watermark on first page      -->
                    <!--   Desktop $495:  single user, local CLI             -->
                    <!--   Server  $3800: gov/non-profit, one server         -->
                    <!-- ════════════════════════════════════════════════════ -->
                    <execution>
                        <id>generate-print-html-prince</id>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>process-asciidoc</goal>
                        </goals>
                        <configuration>
                            <skip>${ike.skip.prince}</skip>
                            <backend>html5</backend>
                            <outputDirectory>${asciidoc.output.directory}/pdf-html-prince</outputDirectory>
                            <extensions>
                                <extension>
                                    <className>network.ike.docs.koncept.KonceptGlossaryProcessor</className>
                                </extension>
                            </extensions>
                            <attributes>
                                <toc>auto</toc>
                                <toclevels>3</toclevels>
                                <linkcss>false</linkcss>
                                <ike-pdf-renderer>prince</ike-pdf-renderer>
                            </attributes>
                        </configuration>
                    </execution>

                    <!-- ════════════════════════════════════════════════════ -->
                    <!-- PDF via Antenna House Formatter (CSS mode)          -->
                    <!--                                                     -->
                    <!-- Pipeline: AsciiDoc → HTML5 → AH Formatter → PDF    -->
                    <!-- AH supports both XSL-FO and CSS Paged Media.       -->
                    <!-- CSS mode used here for consistency.                 -->
                    <!--                                                     -->
                    <!-- License tiers (antennahouse.com/pricing):           -->
                    <!--   Standalone Lite $400:  CSS only, perpetual        -->
                    <!--   Standalone Std  $1250: XSL only, perpetual        -->
                    <!--   XSL/CSS Lite    $560:  both engines, perpetual    -->
                    <!--   XSL/CSS Std     $1750: both engines, perpetual    -->
                    <!-- ════════════════════════════════════════════════════ -->
                    <execution>
                        <id>generate-print-html-ah</id>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>process-asciidoc</goal>
                        </goals>
                        <configuration>
                            <skip>${ike.skip.ah}</skip>
                            <backend>html5</backend>
                            <outputDirectory>${asciidoc.output.directory}/pdf-html-ah</outputDirectory>
                            <extensions>
                                <extension>
                                    <className>network.ike.docs.koncept.KonceptGlossaryProcessor</className>
                                </extension>
                            </extensions>
                            <attributes>
                                <toc>auto</toc>
                                <toclevels>3</toclevels>
                                <linkcss>false</linkcss>
                                <ike-pdf-renderer>ah</ike-pdf-renderer>
                            </attributes>
                        </configuration>
                    </execution>

                    <!-- ════════════════════════════════════════════════════ -->
                    <!-- PDF via WeasyPrint (free, open source, Python)      -->
                    <!--                                                     -->
                    <!-- Pipeline: AsciiDoc → HTML5 → WeasyPrint → PDF      -->
                    <!-- Best free alternative. Good CSS Paged Media.        -->
                    <!-- No JavaScript support (not needed).                 -->
                    <!--                                                     -->
                    <!-- Limitations vs Prince/AH:                           -->
                    <!--   - No target-counter() for TOC page numbers        -->
                    <!--   - No PDF/UA output (tagged PDF limited)           -->
                    <!--   - Requires Python 3.8+ and system libraries       -->
                    <!--   Install: pip install weasyprint                   -->
                    <!-- ════════════════════════════════════════════════════ -->
                    <execution>
                        <id>generate-print-html-weasyprint</id>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>process-asciidoc</goal>
                        </goals>
                        <configuration>
                            <skip>${ike.skip.weasyprint}</skip>
                            <backend>html5</backend>
                            <outputDirectory>${asciidoc.output.directory}/pdf-html-weasyprint</outputDirectory>
                            <!-- Register glossary Postprocessor for HTML only.
                                 The InlineMacro is auto-discovered via SPI.
                                 The Postprocessor cannot be registered via SPI
                                 because it crashes the asciidoctorj-pdf backend. -->
                            <extensions>
                                <extension>
                                    <className>network.ike.docs.koncept.KonceptGlossaryProcessor</className>
                                </extension>
                            </extensions>
                            <attributes>
                                <toc>auto</toc>
                                <toclevels>3</toclevels>
                                <linkcss>false</linkcss>
                                <ike-pdf-renderer>weasyprint</ike-pdf-renderer>
                            </attributes>
                        </configuration>
                    </execution>

                    <!-- ════════════════════════════════════════════════════ -->
                    <!-- PDF via DocBook → XSL-FO → RenderX XEP             -->
                    <!--                                                     -->
                    <!-- Pipeline: AsciiDoc → DocBook5 → XSL-FO → PDF       -->
                    <!--   Step 1: Unpack ike-docbook-xsl from Maven/Nexus  -->
                    <!--   Step 2: asciidoctor docbook5 → DocBook5 XML      -->
                    <!--   Step 3: xml-maven-plugin + Saxon-HE → XSL-FO    -->
                    <!--   Step 4: RenderX XEP → PDF                        -->
                    <!--                                                     -->
                    <!-- 100% Maven-managed. All deps from Nexus.           -->
                    <!-- External prerequisite: XEP installed               -->
                    <!--   (free personal license from renderx.com)         -->
                    <!--                                                     -->
                    <!-- Intermediate outputs (for debugging):               -->
                    <!--   target/generated-docs/docbook-xep/index.xml      -->
                    <!--   target/generated-docs/fo-xep/index.fo            -->
                    <!--   target/generated-docs/pdf-xep/index.pdf          -->
                    <!-- ════════════════════════════════════════════════════ -->
                    <execution>
                        <id>generate-docbook-xep</id>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>process-asciidoc</goal>
                        </goals>
                        <configuration>
                            <skip>${ike.skip.xep}</skip>
                            <backend>docbook5</backend>
                            <outputDirectory>${asciidoc.output.directory}/docbook-xep</outputDirectory>
                            <!-- Register glossary Postprocessor for DocBook.
                                 The InlineMacro is auto-discovered via SPI.
                                 The Postprocessor cannot be registered via SPI
                                 because it crashes the asciidoctorj-pdf backend. -->
                            <extensions>
                                <extension>
                                    <className>network.ike.docs.koncept.KonceptGlossaryProcessor</className>
                                </extension>
                            </extensions>
                            <attributes>
                                <!-- DocBook backend-specific attributes -->
                                <doctype>book</doctype>
                                <allow-uri-read>true</allow-uri-read>
                                <ike-pdf-renderer>xep</ike-pdf-renderer>
                            </attributes>
                        </configuration>
                    </execution>

                    <!-- ════════════════════════════════════════════════════ -->
                    <!-- PDF via DocBook → XSL-FO → Apache FOP              -->
                    <!--                                                     -->
                    <!-- Same DocBook → XSL-FO pipeline as XEP, but uses    -->
                    <!-- Apache FOP (free, pure Java) for the FO→PDF step.  -->
                    <!--                                                     -->
                    <!-- FOP runs as a Java class via exec-maven-plugin.    -->
                    <!-- org.apache.xmlgraphics:fop resolved from Maven     -->
                    <!-- Central — no local binary install needed.          -->
                    <!--                                                     -->
                    <!-- License: Apache License 2.0 (free)                 -->
                    <!--                                                     -->
                    <!-- Intermediate outputs (for debugging):               -->
                    <!--   target/generated-docs/docbook-fop/index.xml      -->
                    <!--   target/generated-docs/fo-fop/index.fo            -->
                    <!--   target/generated-docs/pdf-fop/index.pdf          -->
                    <!-- ════════════════════════════════════════════════════ -->
                    <execution>
                        <id>generate-docbook-fop</id>
                        <phase>prepare-package</phase>
                        <goals>
                            <goal>process-asciidoc</goal>
                        </goals>
                        <configuration>
                            <skip>${ike.skip.fop}</skip>
                            <backend>docbook5</backend>
                            <outputDirectory>${asciidoc.output.directory}/docbook-fop</outputDirectory>
                            <!-- Register glossary Postprocessor for DocBook.
                                 The InlineMacro is auto-discovered via SPI.
                                 The Postprocessor cannot be registered via SPI
                                 because it crashes the asciidoctorj-pdf backend. -->
                            <extensions>
                                <extension>
                                    <className>network.ike.docs.koncept.KonceptGlossaryProcessor</className>
                                </extension>
                            </extensions>
                            <attributes>
                                <!-- DocBook backend-specific attributes -->
                                <doctype>book</doctype>
                                <allow-uri-read>true</allow-uri-read>
                                <ike-pdf-renderer>fop</ike-pdf-renderer>
                            </attributes>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- ── xml-maven-plugin ─────────────────────────────────────── -->
            <!-- DocBook5 XML → XSL-FO via Saxon-HE + DocBook XSL.          -->
            <!-- MUST appear before exec-maven-plugin: within the package    -->
            <!-- phase, the XSLT transform produces .fo files that XEP/FOP   -->
            <!-- (exec-maven-plugin) then consume.                           -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>xml-maven-plugin</artifactId>
                <executions>
                    <!-- XEP: DocBook5 XML → XSL-FO                              -->
                    <!--       Saxon-HE is resolved from Maven Central as a       -->
                    <!--       plugin dependency (declared in pluginManagement).   -->
                    <!--       The transform uses custom/ike-fo.xsl (unpacked     -->
                    <!--       from ike-docbook-xsl artifact), which imports the  -->
                    <!--       stock DocBook FO stylesheet via ../fo/docbook.xsl. -->
                    <execution>
                        <id>docbook-to-fo-xep</id>
                        <phase>package</phase>
                        <goals><goal>transform</goal></goals>
                        <configuration>
                            <skip>${ike.skip.xep}</skip>
                            <transformationSets>
                                <transformationSet>
                                    <dir>${asciidoc.output.directory}/docbook-xep</dir>
                                    <includes>
                                        <include>${pdf.source.document}.xml</include>
                                    </includes>
                                    <stylesheet>${docbook.xsl.directory}/custom/ike-fo.xsl</stylesheet>
                                    <outputDir>${asciidoc.output.directory}/fo-xep</outputDir>
                                    <fileMappers>
                                        <fileMapper implementation="org.codehaus.plexus.components.io.filemappers.FileExtensionMapper">
                                            <targetExtension>.fo</targetExtension>
                                        </fileMapper>
                                    </fileMappers>
                                    <parameters>
                                        <parameter>
                                            <name>ike.pdf.renderer</name>
                                            <value>xep</value>
                                        </parameter>
                                    </parameters>
                                </transformationSet>
                            </transformationSets>
                        </configuration>
                    </execution>

                    <!-- FOP: DocBook5 XML → XSL-FO                              -->
                    <!--       Uses fop1.extensions=1 for FOP-specific PDF        -->
                    <!--       bookmarks and metadata support.                     -->
                    <execution>
                        <id>docbook-to-fo-fop</id>
                        <phase>package</phase>
                        <goals><goal>transform</goal></goals>
                        <configuration>
                            <skip>${ike.skip.fop}</skip>
                            <transformationSets>
                                <transformationSet>
                                    <dir>${asciidoc.output.directory}/docbook-fop</dir>
                                    <includes>
                                        <include>${pdf.source.document}.xml</include>
                                    </includes>
                                    <stylesheet>${docbook.xsl.directory}/custom/ike-fo.xsl</stylesheet>
                                    <outputDir>${asciidoc.output.directory}/fo-fop</outputDir>
                                    <fileMappers>
                                        <fileMapper implementation="org.codehaus.plexus.components.io.filemappers.FileExtensionMapper">
                                            <targetExtension>.fo</targetExtension>
                                        </fileMapper>
                                    </fileMappers>
                                    <parameters>
                                        <parameter>
                                            <name>fop1.extensions</name>
                                            <value>1</value>
                                        </parameter>
                                        <parameter>
                                            <name>ike.pdf.renderer</name>
                                            <value>fop</value>
                                        </parameter>
                                    </parameters>
                                </transformationSet>
                            </transformationSets>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- ── exec-maven-plugin ────────────────────────────────────── -->
            <!-- External renderer invocations: PDF rendering via Prince,   -->
            <!-- AH, WeasyPrint, XEP, and FOP. File renames and copies.     -->
            <!-- MUST appear after xml-maven-plugin: XEP/FOP executions in  -->
            <!-- the package phase read .fo files produced by xml-maven's   -->
            <!-- XSLT transforms. Maven runs same-phase plugins in POM     -->
            <!-- declaration order.                                         -->
            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <executions>

                    <!-- Create PDF output directories for external renderers.   -->
                    <!-- The renderers cannot create these directories themselves -->
                    <!-- and will fail if they don't exist.                       -->
                    <execution>
                        <id>mkdir-pdf-prince</id>
                        <phase>prepare-package</phase>
                        <goals><goal>exec</goal></goals>
                        <configuration>
                            <skip>${ike.skip.prince}</skip>
                            <executable>mkdir</executable>
                            <arguments><argument>-p</argument><argument>${asciidoc.output.directory}/pdf-prince</argument></arguments>
                        </configuration>
                    </execution>
                    <execution>
                        <id>mkdir-pdf-ah</id>
                        <phase>prepare-package</phase>
                        <goals><goal>exec</goal></goals>
                        <configuration>
                            <skip>${ike.skip.ah}</skip>
                            <executable>mkdir</executable>
                            <arguments><argument>-p</argument><argument>${asciidoc.output.directory}/pdf-ah</argument></arguments>
                        </configuration>
                    </execution>
                    <execution>
                        <id>mkdir-pdf-weasyprint</id>
                        <phase>prepare-package</phase>
                        <goals><goal>exec</goal></goals>
                        <configuration>
                            <skip>${ike.skip.weasyprint}</skip>
                            <executable>mkdir</executable>
                            <arguments><argument>-p</argument><argument>${asciidoc.output.directory}/pdf-weasyprint</argument></arguments>
                        </configuration>
                    </execution>
                    <execution>
                        <id>mkdir-pdf-xep</id>
                        <phase>prepare-package</phase>
                        <goals><goal>exec</goal></goals>
                        <configuration>
                            <skip>${ike.skip.xep}</skip>
                            <executable>mkdir</executable>
                            <arguments><argument>-p</argument><argument>${asciidoc.output.directory}/pdf-xep</argument></arguments>
                        </configuration>
                    </execution>
                    <execution>
                        <id>mkdir-pdf-fop</id>
                        <phase>prepare-package</phase>
                        <goals><goal>exec</goal></goals>
                        <configuration>
                            <skip>${ike.skip.fop}</skip>
                            <executable>mkdir</executable>
                            <arguments><argument>-p</argument><argument>${asciidoc.output.directory}/pdf-fop</argument></arguments>
                        </configuration>
                    </execution>

                    <!-- Fix bare <rect/> elements in Mermaid-generated inline   -->
                    <!-- SVGs before PDF renderers process the HTML.                -->
                    <execution>
                        <id>fix-svg-prince</id>
                        <phase>prepare-package</phase>
                        <goals><goal>exec</goal></goals>
                        <configuration>
                            <skip>${ike.skip.prince}</skip>
                            <executable>sh</executable>
                            <arguments>
                                <argument>${project.build.directory}/build-tools/scripts/fix-inline-svg.sh</argument>
                                <argument>${asciidoc.output.directory}/pdf-html-prince/${pdf.source.document}.html</argument>
                            </arguments>
                        </configuration>
                    </execution>

                    <!-- Prince: HTML + CSS → PDF -->
                    <execution>
                        <id>prince-html-to-pdf</id>
                        <phase>package</phase>
                        <goals><goal>exec</goal></goals>
                        <configuration>
                            <skip>${ike.skip.prince}</skip>
                            <outputFile>${project.build.directory}/logs/renderer-prince.log</outputFile>
                            <executable>${prince.executable}</executable>
                            <arguments>
                                <argument>${asciidoc.output.directory}/pdf-html-prince/${pdf.source.document}.html</argument>
                                <argument>--style</argument>
                                <argument>${asciidoc.output.directory}/pdf-html-prince/ike-print.css</argument>
                                <argument>--output</argument>
                                <argument>${asciidoc.output.directory}/pdf-prince/${ike.document.name}.pdf</argument>
                                <argument>--pdf-profile=PDF/UA-1</argument>
                            </arguments>
                        </configuration>
                    </execution>

                    <execution>
                        <id>fix-svg-ah</id>
                        <phase>prepare-package</phase>
                        <goals><goal>exec</goal></goals>
                        <configuration>
                            <skip>${ike.skip.ah}</skip>
                            <executable>sh</executable>
                            <arguments>
                                <argument>${project.build.directory}/build-tools/scripts/fix-inline-svg.sh</argument>
                                <argument>${asciidoc.output.directory}/pdf-html-ah/${pdf.source.document}.html</argument>
                            </arguments>
                        </configuration>
                    </execution>

                    <!-- AH: HTML + CSS → PDF                                -->
                    <!--     -cssmode: use CSS engine (not XSL-FO)           -->
                    <!--     -p @PDF/UA-1: accessible PDF output             -->
                    <execution>
                        <id>ah-html-to-pdf</id>
                        <phase>package</phase>
                        <goals><goal>exec</goal></goals>
                        <configuration>
                            <skip>${ike.skip.ah}</skip>
                            <outputFile>${project.build.directory}/logs/renderer-ah.log</outputFile>
                            <executable>${antennahouse.executable}</executable>
                            <arguments>
                                <argument>-cssmode</argument>
                                <argument>-css</argument>
                                <argument>${asciidoc.output.directory}/pdf-html-ah/ike-print.css</argument>
                                <argument>-d</argument>
                                <argument>${asciidoc.output.directory}/pdf-html-ah/${pdf.source.document}.html</argument>
                                <argument>-o</argument>
                                <argument>${asciidoc.output.directory}/pdf-ah/${ike.document.name}.pdf</argument>
                                <argument>-p</argument>
                                <argument>@PDF/UA-1</argument>
                            </arguments>
                        </configuration>
                    </execution>

                    <execution>
                        <id>fix-svg-weasyprint</id>
                        <phase>prepare-package</phase>
                        <goals><goal>exec</goal></goals>
                        <configuration>
                            <skip>${ike.skip.weasyprint}</skip>
                            <executable>sh</executable>
                            <arguments>
                                <argument>${project.build.directory}/build-tools/scripts/fix-inline-svg.sh</argument>
                                <argument>${asciidoc.output.directory}/pdf-html-weasyprint/${pdf.source.document}.html</argument>
                            </arguments>
                        </configuration>
                    </execution>

                    <!-- WeasyPrint: HTML + CSS → PDF -->
                    <execution>
                        <id>weasyprint-html-to-pdf</id>
                        <phase>package</phase>
                        <goals><goal>exec</goal></goals>
                        <configuration>
                            <skip>${ike.skip.weasyprint}</skip>
                            <outputFile>${project.build.directory}/logs/renderer-weasyprint.log</outputFile>
                            <executable>${weasyprint.executable}</executable>
                            <arguments>
                                <argument>${asciidoc.output.directory}/pdf-html-weasyprint/${pdf.source.document}.html</argument>
                                <argument>${asciidoc.output.directory}/pdf-weasyprint/${ike.document.name}.pdf</argument>
                                <argument>--stylesheet</argument>
                                <argument>${asciidoc.output.directory}/pdf-html-weasyprint/ike-print.css</argument>
                            </arguments>
                        </configuration>
                    </execution>

                    <!-- XEP: XSL-FO → PDF via RenderX XEP -->
                    <!--       Invoke Java directly (not the xep wrapper) so we can    -->
                    <!--       pass -Dcom.renderx.xep.CONFIG pointing to our custom    -->
                    <!--       config that registers Noto fonts.                       -->
                    <execution>
                        <id>xep-fo-to-pdf</id>
                        <phase>package</phase>
                        <goals><goal>exec</goal></goals>
                        <configuration>
                            <skip>${ike.skip.xep}</skip>
                            <outputFile>${project.build.directory}/logs/renderer-xep.log</outputFile>
                            <executable>java</executable>
                            <arguments>
                                <argument>-classpath</argument>
                                <argument>${xep.home}/lib/xep.jar:${xep.home}/lib/saxon.jar:${xep.home}/lib/saxon-xml-apis.jar</argument>
                                <argument>-Dcom.renderx.xep.CONFIG=${project.build.directory}/xep-config/xep-ike.xml</argument>
                                <argument>com.renderx.xep.XSLDriver</argument>
                                <argument>-fo</argument>
                                <argument>${asciidoc.output.directory}/fo-xep/${pdf.source.document}.fo</argument>
                                <argument>-pdf</argument>
                                <argument>${asciidoc.output.directory}/pdf-xep/${ike.document.name}.pdf</argument>
                            </arguments>
                        </configuration>
                    </execution>

                    <!-- FOP: XSL-FO → PDF via Apache FOP (separate JVM)          -->
                    <!--       FOP's Main.main() calls System.exit(), so we run it -->
                    <!--       as exec:exec (not exec:java) to avoid killing the   -->
                    <!--       Maven JVM. The classpath is supplied from the        -->
                    <!--       pdf-fop profile's <dependencies>.                   -->
                    <execution>
                        <id>fop-fo-to-pdf</id>
                        <phase>package</phase>
                        <goals><goal>exec</goal></goals>
                        <configuration>
                            <skip>${ike.skip.fop}</skip>
                            <outputFile>${project.build.directory}/logs/renderer-fop.log</outputFile>
                            <executable>java</executable>
                            <arguments>
                                <argument>-classpath</argument>
                                <classpath/>
                                <argument>org.apache.fop.cli.Main</argument>
                                <argument>-r</argument>
                                <argument>-c</argument>
                                <argument>${project.build.directory}/fop-config/fop-ike.xconf</argument>
                                <argument>-fo</argument>
                                <argument>${asciidoc.output.directory}/fo-fop/${pdf.source.document}.fo</argument>
                                <argument>-pdf</argument>
                                <argument>${asciidoc.output.directory}/pdf-fop/${ike.document.name}.pdf</argument>
                            </arguments>
                        </configuration>
                    </execution>

                    <!-- html-single: Rename index.html → ${ike.document.name}.html -->
                    <!-- Asciidoctor names output after the source file. This      -->
                    <!-- rename gives the self-contained HTML a meaningful name.   -->
                    <execution>
                        <id>rename-html-single</id>
                        <phase>verify</phase>
                        <goals><goal>exec</goal></goals>
                        <configuration>
                            <skip>${ike.skip.html-single}</skip>
                            <executable>mv</executable>
                            <arguments>
                                <argument>${asciidoc.output.directory}/html-single/${pdf.source.document}.html</argument>
                                <argument>${asciidoc.output.directory}/html-single/${ike.document.name}.html</argument>
                            </arguments>
                        </configuration>
                    </execution>

                    <!-- Prawn: Rename index.pdf → ${ike.document.name}.pdf      -->
                    <!-- asciidoctorj-pdf always names output after the source    -->
                    <!-- file. This rename gives the PDF a meaningful name.       -->
                    <!-- Runs in verify phase after asciidoctor (Prawn) because   -->
                    <!-- exec-maven-plugin is declared after asciidoctor-maven-   -->
                    <!-- plugin in the POM.                                       -->
                    <execution>
                        <id>rename-prawn-pdf</id>
                        <phase>verify</phase>
                        <goals><goal>exec</goal></goals>
                        <configuration>
                            <skip>${ike.skip.prawn}</skip>
                            <executable>mv</executable>
                            <arguments>
                                <argument>${asciidoc.output.directory}/pdf-prawn/${pdf.source.document}.pdf</argument>
                                <argument>${asciidoc.output.directory}/pdf-prawn/${ike.document.name}.pdf</argument>
                            </arguments>
                        </configuration>
                    </execution>

                    <!-- pdf-default: Copy the designated renderer's PDF to pdf/ -->
                    <!-- Uses verify phase — runs after asciidoctor (Prawn) and  -->
                    <!-- after all package-phase renderers have finished.         -->
                    <!-- exec-maven-plugin is declared after asciidoctor-maven-   -->
                    <!-- plugin so within verify phase this runs after Prawn PDF  -->
                    <!-- generation completes.                                    -->
                    <execution>
                        <id>mkdir-pdf-default</id>
                        <phase>verify</phase>
                        <goals><goal>exec</goal></goals>
                        <configuration>
                            <skip>${ike.skip.pdf-default}</skip>
                            <executable>mkdir</executable>
                            <arguments>
                                <argument>-p</argument>
                                <argument>${asciidoc.output.directory}/pdf</argument>
                            </arguments>
                        </configuration>
                    </execution>
                    <execution>
                        <id>copy-default-pdf</id>
                        <phase>verify</phase>
                        <goals><goal>exec</goal></goals>
                        <configuration>
                            <skip>${ike.skip.pdf-default}</skip>
                            <executable>cp</executable>
                            <arguments>
                                <argument>${asciidoc.output.directory}/pdf-${ike.pdf.default}/${ike.document.name}.pdf</argument>
                                <argument>${asciidoc.output.directory}/pdf/</argument>
                            </arguments>
                        </configuration>
                    </execution>

                    <!-- Scan renderer log files for errors and print summary.  -->
                    <!-- Runs in verify phase after all renderers (package) and -->
                    <!-- after pdf-default copy. Always enabled — the script    -->
                    <!-- silently exits if no log files exist.                  -->
                    <execution>
                        <id>scan-renderer-logs</id>
                        <phase>verify</phase>
                        <goals><goal>exec</goal></goals>
                        <configuration>
                            <executable>sh</executable>
                            <arguments>
                                <argument>${project.build.directory}/build-tools/scripts/scan-renderer-logs.sh</argument>
                                <argument>${project.build.directory}/logs</argument>
                            </arguments>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- ── maven-assembly-plugin ────────────────────────────────── -->
            <!-- Create classified ZIP artifacts for each renderer output    -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-assembly-plugin</artifactId>
                <executions>
                    <!-- HTML ZIP (verify phase — after HTML generation) -->
                    <execution>
                        <id>zip-html</id>
                        <phase>verify</phase>
                        <goals><goal>single</goal></goals>
                        <configuration>
                            <skipAssembly>${ike.skip.html}</skipAssembly>
                            <descriptors>
                                <descriptor>${ike.assembly.directory}/html.xml</descriptor>
                            </descriptors>
                        </configuration>
                    </execution>

                    <!-- Self-contained HTML ZIP (verify phase) -->
                    <execution>
                        <id>zip-html-single</id>
                        <phase>verify</phase>
                        <goals><goal>single</goal></goals>
                        <configuration>
                            <skipAssembly>${ike.skip.html-single}</skipAssembly>
                            <descriptors>
                                <descriptor>${ike.assembly.directory}/html-single.xml</descriptor>
                            </descriptors>
                        </configuration>
                    </execution>

                    <!-- Prawn PDF ZIP (verify phase — after Prawn generation) -->
                    <execution>
                        <id>zip-prawn</id>
                        <phase>verify</phase>
                        <goals><goal>single</goal></goals>
                        <configuration>
                            <skipAssembly>${ike.skip.prawn}</skipAssembly>
                            <descriptors>
                                <descriptor>${ike.assembly.directory}/prawn.xml</descriptor>
                            </descriptors>
                        </configuration>
                    </execution>

                    <!-- Prince PDF ZIP -->
                    <execution>
                        <id>zip-prince</id>
                        <phase>package</phase>
                        <goals><goal>single</goal></goals>
                        <configuration>
                            <skipAssembly>${ike.skip.prince}</skipAssembly>
                            <descriptors>
                                <descriptor>${ike.assembly.directory}/prince.xml</descriptor>
                            </descriptors>
                        </configuration>
                    </execution>

                    <!-- AH PDF ZIP -->
                    <execution>
                        <id>zip-ah</id>
                        <phase>package</phase>
                        <goals><goal>single</goal></goals>
                        <configuration>
                            <skipAssembly>${ike.skip.ah}</skipAssembly>
                            <descriptors>
                                <descriptor>${ike.assembly.directory}/ah.xml</descriptor>
                            </descriptors>
                        </configuration>
                    </execution>

                    <!-- WeasyPrint PDF ZIP -->
                    <execution>
                        <id>zip-weasyprint</id>
                        <phase>package</phase>
                        <goals><goal>single</goal></goals>
                        <configuration>
                            <skipAssembly>${ike.skip.weasyprint}</skipAssembly>
                            <descriptors>
                                <descriptor>${ike.assembly.directory}/weasyprint.xml</descriptor>
                            </descriptors>
                        </configuration>
                    </execution>

                    <!-- XEP PDF ZIP -->
                    <execution>
                        <id>zip-xep</id>
                        <phase>package</phase>
                        <goals><goal>single</goal></goals>
                        <configuration>
                            <skipAssembly>${ike.skip.xep}</skipAssembly>
                            <descriptors>
                                <descriptor>${ike.assembly.directory}/xep.xml</descriptor>
                            </descriptors>
                        </configuration>
                    </execution>

                    <!-- FOP PDF ZIP -->
                    <execution>
                        <id>zip-fop</id>
                        <phase>package</phase>
                        <goals><goal>single</goal></goals>
                        <configuration>
                            <skipAssembly>${ike.skip.fop}</skipAssembly>
                            <descriptors>
                                <descriptor>${ike.assembly.directory}/fop.xml</descriptor>
                            </descriptors>
                        </configuration>
                    </execution>

                    <!-- Default PDF copy ZIP (verify phase) -->
                    <execution>
                        <id>zip-pdf-default</id>
                        <phase>verify</phase>
                        <goals><goal>single</goal></goals>
                        <configuration>
                            <skipAssembly>${ike.skip.pdf-default}</skipAssembly>
                            <descriptors>
                                <descriptor>${ike.assembly.directory}/pdf.xml</descriptor>
                            </descriptors>
                        </configuration>
                    </execution>

                    <!-- AsciiDoc source ZIP for topic libraries -->
                    <execution>
                        <id>zip-asciidoc</id>
                        <phase>package</phase>
                        <goals><goal>single</goal></goals>
                        <configuration>
                            <skipAssembly>${ike.skip.asciidoc-zip}</skipAssembly>
                            <descriptors>
                                <descriptor>${ike.assembly.directory}/asciidoc.xml</descriptor>
                            </descriptors>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <!-- ════════════════════════════════════════════════════════════════ -->
    <!-- Profiles — thin toggles that flip renderer skip flags.          -->
    <!--                                                                 -->
    <!-- Each profile sets its ike.skip.* property to false.             -->
    <!-- All build logic lives in the main <build> section above.        -->
    <!-- Profiles are composable: activate multiple renderers by         -->
    <!-- passing multiple -D properties or -P flags.                     -->
    <!-- ════════════════════════════════════════════════════════════════ -->
    <profiles>
        <!-- ── HTML5 (active by default) ──────────────────────────────── -->
        <profile>
            <id>html</id>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
            <properties>
                <ike.skip.html>false</ike.skip.html>
            </properties>
        </profile>

        <!-- ── Self-contained HTML (single file, data-uri) ─────────────── -->
        <profile>
            <id>html-single</id>
            <activation>
                <property><name>ike.html.single</name></property>
            </activation>
            <properties>
                <ike.skip.html-single>false</ike.skip.html-single>
            </properties>
        </profile>

        <!-- ── PDF via Prawn (built-in, free) ─────────────────────────── -->
        <profile>
            <id>pdf</id>
            <activation>
                <property><name>ike.pdf.prawn</name></property>
            </activation>
            <properties>
                <ike.skip.prawn>false</ike.skip.prawn>
            </properties>
        </profile>

        <!-- ── PDF via Prince XML ─────────────────────────────────────── -->
        <profile>
            <id>pdf-prince</id>
            <activation>
                <property><name>ike.pdf.prince</name></property>
            </activation>
            <properties>
                <ike.skip.prince>false</ike.skip.prince>
            </properties>
        </profile>

        <!-- ── PDF via Antenna House Formatter ────────────────────────── -->
        <profile>
            <id>pdf-ah</id>
            <activation>
                <property><name>ike.pdf.ah</name></property>
            </activation>
            <properties>
                <ike.skip.ah>false</ike.skip.ah>
            </properties>
        </profile>

        <!-- ── PDF via WeasyPrint ─────────────────────────────────────── -->
        <profile>
            <id>pdf-weasyprint</id>
            <activation>
                <property><name>ike.pdf.weasyprint</name></property>
            </activation>
            <properties>
                <ike.skip.weasyprint>false</ike.skip.weasyprint>
            </properties>
        </profile>

        <!-- ── PDF via DocBook → XSL-FO → RenderX XEP ────────────────── -->
        <profile>
            <id>pdf-xep</id>
            <activation>
                <property><name>ike.pdf.xep</name></property>
            </activation>
            <properties>
                <ike.skip.xep>false</ike.skip.xep>
                <ike.skip.docbook>false</ike.skip.docbook>
            </properties>
        </profile>

        <!-- ── PDF via DocBook → XSL-FO → Apache FOP ─────────────────── -->
        <profile>
            <id>pdf-fop</id>
            <activation>
                <property><name>ike.pdf.fop</name></property>
            </activation>
            <properties>
                <ike.skip.fop>false</ike.skip.fop>
                <ike.skip.docbook>false</ike.skip.docbook>
            </properties>
            <dependencies>
                <!-- FOP on project classpath so exec:exec can build -classpath.
                     Runtime scope: not needed for compilation, only for the
                     fop-fo-to-pdf execution (separate JVM via exec:exec). -->
                <dependency>
                    <groupId>org.apache.xmlgraphics</groupId>
                    <artifactId>fop</artifactId>
                    <version>${fop.version}</version>
                    <scope>runtime</scope>
                </dependency>
            </dependencies>
        </profile>

        <!-- ── Default PDF copy ───────────────────────────────────────── -->
        <profile>
            <id>pdf-default</id>
            <activation>
                <property><name>ike.pdf.default</name></property>
            </activation>
            <properties>
                <ike.skip.pdf-default>false</ike.skip.pdf-default>
            </properties>
        </profile>

        <!-- ── Skip all renderers on POM-packaging modules ─────────── -->
        <!-- POM modules (java-parent, ike-parent) typically    -->
        <!-- have no AsciiDoc content or lack the theme/font setup that   -->
        <!-- renderers require. This profile forces ALL skip flags to     -->
        <!-- true. To build docs from a POM module, override explicitly:  -->
        <!--   mvn verify -pl ike-parent -Dike.skip.html=false  -->
        <!-- Must appear AFTER renderer profiles so its properties win.   -->
        <profile>
            <id>pom-skip-renderers</id>
            <activation>
                <packaging>pom</packaging>
            </activation>
            <properties>
                <ike.skip.html>true</ike.skip.html>
                <ike.skip.html-single>true</ike.skip.html-single>
                <ike.skip.prawn>true</ike.skip.prawn>
                <ike.skip.prince>true</ike.skip.prince>
                <ike.skip.ah>true</ike.skip.ah>
                <ike.skip.weasyprint>true</ike.skip.weasyprint>
                <ike.skip.xep>true</ike.skip.xep>
                <ike.skip.fop>true</ike.skip.fop>
                <ike.skip.pdf-default>true</ike.skip.pdf-default>
                <ike.skip.docbook>true</ike.skip.docbook>
            </properties>
        </profile>

        <!-- ── Diff Generation (stub for future implementation) ───────── -->
        <profile>
            <id>diff-generation</id>
            <build>
                <plugins>
                    <!--
                    Placeholder for future git-diff based documentation change tracking.
                    Potential approaches:
                    - JGit-based Maven plugin to generate annotated diffs
                    - AsciiDoc preprocessing to mark changed sections
                    - Integration with conventional-changelog style tools
                    -->
                </plugins>
            </build>
        </profile>
    </profiles>

</project>
